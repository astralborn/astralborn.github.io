[pytest]
pythonpath = .
testpaths = tests
# Run every test against all three Playwright browser engines.
# Individual runs can still override with: pytest --browser chromium
addopts = --browser chromium --browser firefox --browser webkit

